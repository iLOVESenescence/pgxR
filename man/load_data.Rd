% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{load_data}
\alias{load_data}
\title{Load and preprocess dose-response data}
\usage{
load_data(filepath, col_map = NULL)
}
\arguments{
\item{filepath}{Character. Path to the input CSV file.}

\item{col_map}{Named list mapping pgxR's expected column names to the
actual column names in your CSV. Only specify columns that differ.
For example, if your CSV has \code{translocation} instead of \code{feature}:
\code{col_map = list(feature = "translocation")}.
Or if ancestry is stored as \code{population}:
\code{col_map = list(ancestry = "population", feature = "translocation")}.}
}
\value{
A data frame with \code{dose} and \code{response} as numeric, and
\code{cell_line}, \code{ancestry}, \code{feature} as factors.
}
\description{
Reads a CSV and coerces columns to expected types. Negative response values
are clamped to 0 before any downstream fitting.
}
\details{
The input CSV must contain the following columns:
\itemize{
\item \code{dose} — numeric drug concentration
\item \code{response} — numeric viability or death response (0–100 scale expected)
\item \code{cell_line} — cell line identifier
\item \code{ancestry} — population/ancestry label
\item \code{feature} — translocation or mutation status (use \code{"none"} if absent)
}
}
\examples{
\dontrun{
raw <- load_data("drug_response_data.csv")
}
}
