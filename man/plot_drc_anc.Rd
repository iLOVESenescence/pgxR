% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_drc.R
\name{plot_drc_anc}
\alias{plot_drc_anc}
\title{Plot dose-response curves grouped by ancestry and faceted by feature}
\usage{
plot_drc_anc(
  agg_data,
  pred_data,
  title = "",
  y_label = "Effect (\%)",
  x_label = "Dose",
  ancestry_colors = ANCESTRY_COLORS,
  ancestry_linetypes = ANCESTRY_LINETYPES,
  label_size = 3
)
}
\arguments{
\item{agg_data}{Aggregated data frame from \code{\link[=combine_reps]{combine_reps()}}.}

\item{pred_data}{Prediction data frame from \code{\link[=predict_drc]{predict_drc()}}.}

\item{title}{Character. Plot title. Default \code{""}.}

\item{y_label}{Character. Y-axis label. Default \code{"Effect (\%)"}.}

\item{x_label}{Character. X-axis label. Default \code{"Dose"}.}

\item{ancestry_colors}{Named character vector of hex colors per ancestry
group. Default \link{ANCESTRY_COLORS}.}

\item{ancestry_linetypes}{Named character vector of linetypes per ancestry
group. Default \link{ANCESTRY_LINETYPES}.}

\item{label_size}{Numeric. Font size for cell line labels. Default \code{3}.}
}
\value{
A \code{ggplot} object.
}
\description{
Color and linetype both encode ancestry. Individual cell lines are labeled
at the highest dose point using \code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}. Panels are
faceted by feature group.
}
\examples{
\dontrun{
p <- plot_drc_anc(agg, preds, title = "Drug Response by Ancestry")
}
}
