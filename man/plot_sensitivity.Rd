% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ic50.R
\name{plot_sensitivity}
\alias{plot_sensitivity}
\title{Plot sensitivity metric by group with statistical comparisons}
\usage{
plot_sensitivity(
  df,
  group_col,
  metric = "IC50",
  colors = NULL,
  comparisons = NULL,
  y_label = NULL,
  title = ""
)
}
\arguments{
\item{df}{A data frame containing the sensitivity metric and grouping
column. Typically output from \code{\link[=summarize_drc]{summarize_drc()}} or \code{\link[=extract_all]{extract_all()}}.}

\item{group_col}{Character. Name of the grouping column (e.g. \code{"ancestry"},
\code{"feature"}).}

\item{metric}{Character. Name of the sensitivity metric column to plot.
Default \code{"IC50"}.}

\item{colors}{Optional ggplot2 fill/color scale. Default \code{NULL}.}

\item{comparisons}{Optional list of group pairs for p-value annotation.
If \code{NULL} (default) all pairwise comparisons are shown.}

\item{y_label}{Character. Y-axis label. Default uses \code{metric} value.}

\item{title}{Character. Plot title. Default \code{""}.}
}
\value{
A \code{ggplot} object.
}
\description{
Displays individual cell line values as jittered points with mean and
confidence interval bars. Pairwise p-values are annotated between groups
using \code{\link[ggpubr:stat_compare_means]{ggpubr::stat_compare_means()}}. Works for any grouping variable
(ancestry, feature, mutation status etc.) and any sensitivity metric
(IC50, AUC, hill_slope).
}
\examples{
\dontrun{
# IC50 by ancestry
plot_sensitivity(metrics, group_col = "ancestry", metric = "IC50")

# AUC by feature
plot_sensitivity(metrics, group_col = "feature", metric = "AUC")
}
}
