% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitting.R
\name{fitALL}
\alias{fitALL}
\title{Fit dose-response models for all cell lines}
\usage{
fitALL(data, cell_lines, model = drc::LL.4())
}
\arguments{
\item{data}{Aggregated data frame from \code{\link[=combineReps]{combineReps()}}.}

\item{cell_lines}{Character vector of cell line names. Typically
\code{unique(agg$cell_line)}.}

\item{model}{A \code{drc} model function passed to \code{\link[=fitCL]{fitCL()}}.
Default \code{drc::LL.4()}.}
}
\value{
A named list of \code{drc} model objects, one entry per cell line.
}
\description{
Iterates over a character vector of cell line names and calls
\code{\link[=fitCL]{fitCL()}} for each, returning a named list of \code{drc} model objects.
}
\examples{
\dontrun{
agg  <- combineReps(loadData("data.csv"))
fits <- fitALL(agg, unique(agg$cell_line))
}
}
