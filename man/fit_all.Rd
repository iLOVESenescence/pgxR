% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitting.R
\name{fit_all}
\alias{fit_all}
\title{Fit dose-response models for all cell lines}
\usage{
fit_all(data, cell_lines, model = drc::LL.4())
}
\arguments{
\item{data}{Aggregated data frame from \code{\link[=combine_reps]{combine_reps()}}.}

\item{cell_lines}{Character vector of cell line names. Typically
\code{unique(agg$cell_line)}.}

\item{model}{A \code{drc} model function passed to \code{\link[=fit_cl]{fit_cl()}}.
Default \code{drc::LL.4()}.}
}
\value{
A named list of \code{drc} model objects, one entry per cell line.
}
\description{
Iterates over a character vector of cell line names and calls
\code{\link[=fit_cl]{fit_cl()}} for each, returning a named list of \code{drc} model objects.
}
\examples{
\dontrun{
agg  <- combine_reps(load_data("data.csv"))
fits <- fit_all(agg, unique(agg$cell_line))
}
}
