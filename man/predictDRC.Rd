% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predictDRC}
\alias{predictDRC}
\title{Generate smooth model predictions across the dose range}
\usage{
predictDRC(fits, agg_data, n_points = 200)
}
\arguments{
\item{fits}{Named list of \code{drc} model objects from \code{\link[=fitALL]{fitALL()}}.}

\item{agg_data}{Aggregated data frame from \code{\link[=combineReps]{combineReps()}}.}

\item{n_points}{Integer. Number of evenly-spaced dose points per cell line
over the observed dose range. Default \code{200}.}
}
\value{
A data frame with columns \code{dose}, \code{cell_line},
\code{predicted_response}, \code{ancestry}, and \code{feature}.
}
\description{
Produces a tidy data frame of predicted responses for plotting
fitted curves. Cell line metadata (\code{ancestry}, \code{feature}) is joined
from \code{agg_data} so it propagates automatically to all downstream plot
functions.
}
\examples{
\dontrun{
agg   <- combineReps(loadData("data.csv"))
fits  <- fitALL(agg, unique(agg$cell_line))
preds <- predictDRC(fits, agg)
}
}
